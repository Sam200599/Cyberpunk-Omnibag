import * as INV from "./inventory.js"; import { rollXdY, choiceWeighted } from "./utils.js"; let selectedFolders=[]; export function init(){ console.log('[Loot] init'); } export function importFolders(json){ try{ selectedFolders = JSON.parse(json); return true;}catch(e){return false;} } export function exportFolders(){ return JSON.stringify(selectedFolders,null,2); } export default { init, importFolders, exportFolders };